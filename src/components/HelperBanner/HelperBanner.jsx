import React from 'react';
import './HelperBanner.css';

const HelperBanner = () => {
    return <div className='helper-container'>
        <p>Use W, A, S, D to move around. Click and move the mouse to move the camera. Pinch to zoom in and out.</p>
    </div>
}

export default HelperBanner;